#include<stdio.h>
#include<string.h>
int main()
{
    int n;
 char password=[];
printf("Enter the password");
for(i=0;i<=n;i++){
scanf("%s",&password);}
le=strlen(password);
le<=5?printf("weak"):printf("strong in only length");
switch(password){
case 1
}
}
